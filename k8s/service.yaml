apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: commerce
  labels:
    app: order-service
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  sessionAffinity: None

# NOTE: MariaDB Service는 mariadb-deployment.yaml에 정의되어 있습니다.
